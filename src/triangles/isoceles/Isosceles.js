import React from "react";
import { style } from "../triangle"

const Isosceles = props => {
  const { width, height, bgColor, content } = props;
  return (
    <div style= {{ style  }}
     >
      {content}
    </div>
  );
};

export default Isosceles;
